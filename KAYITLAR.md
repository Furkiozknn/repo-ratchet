### Round 1

27 repositories, 27 advanced, 106 checks recorded, 83 returned what they were run to return.

| Repository | Outcome | What changed / why not | Verified by |
| --- | --- | --- | --- |
| [Furkiozknn.github.io](https://github.com/Furkiozknn/Furkiozknn.github.io) | advanced | 37 tests over the generator, which found and fixed a live crash: a null summary in any repository's metadata took the whole site build down with a TypeError. CI on three Python versions plus a job that rebuilds the real page from the checked-in snapshot and validates the feed, sitemap and JSON-LD. | `python3 -m pytest tests -q` → 0<br>`python3 uret.py --yerel --cikti /tmp/out.html` → 0<br>`gh api repos/Furkiozknn/Furkiozknn.github.io/actions/runs/35753650812/jobs` → 0 |
| [godot-2d-sablon](https://github.com/Furkiozknn/godot-2d-sablon) | advanced | 21 behavioural tests over the jump simulation, deriving every expectation from the constants in oyuncu.gd rather than freezing a number, plus a CI job that runs godot-refcheck over both templates and uploads SARIF. | `python3 -m pytest testler -q` → 0<br>`python3 arac/dogrula.py` → 0<br>`godot-refcheck --recursive .` → 0<br>`gh api repos/Furkiozknn/godot-2d-sablon/actions/runs/35753657775/jobs` → 0 |
| [turkce-ajanlar](https://github.com/Furkiozknn/turkce-ajanlar) | advanced | A routing gate the roster did not have: with 70 agents nothing stopped two of them claiming the same trigger phrase, so the user could say the right sentence and get the wrong agent with every file still passing. It found two real collisions, both fixed. Plus the one bug class no agent owned: date and time, with the Turkey-specific stale-tzdata trap. | `node arac/tetik-cakisma.js` → 0<br>`node arac/tetik-cakisma-test.js` → 0<br>`node arac/dogrula.js && node arac/sinir-denetle.js --kati` → 0<br>`node arac/disari-aktar.js` → 0<br>`gh api repos/Furkiozknn/turkce-ajanlar/actions/runs/35754647583` → 0 |
| [buradane](https://github.com/Furkiozknn/buradane) | advanced | The migration plan in docs/api-sozlesme-farklari.md is now executable: 10 tests read both schemas from source and compare the real gap against the documented one. It found a field the frontend uses in two places and the backend produces nowhere, which the plan did not list. | `python3 -m pytest backend/tests/test_sozlesme_dokumani.py -q` → 0<br>`gh api repos/Furkiozknn/buradane/actions/runs/35755193867` → 0 |
| [tek-tus-kosu](https://github.com/Furkiozknn/tek-tus-kosu) | advanced | A gate the game's own suite could not be: its tests measure play, not whether every scene still points at a file that exists, and the engine does not always say so at startup. godot-refcheck now runs in CI without downloading the engine and uploads SARIF. 222 files, 215 references, clean. | `godot-refcheck tek-tus-kosu` → 0<br>`gh api repos/Furkiozknn/tek-tus-kosu/actions/runs/35756493521` → 0 |
| [yercekimi-cevir](https://github.com/Furkiozknn/yercekimi-cevir) | advanced | A gate the game's own suite could not be: its tests measure play, not whether every scene still points at a file that exists, and the engine does not always say so at startup. godot-refcheck now runs in CI without downloading the engine and uploads SARIF. 252 files, 75 references, clean. | `godot-refcheck yercekimi-cevir` → 0<br>`gh api repos/Furkiozknn/yercekimi-cevir/actions/runs/35756515112` → 0 |
| [kanca](https://github.com/Furkiozknn/kanca) | advanced | A gate the game's own suite could not be: its tests measure play, not whether every scene still points at a file that exists, and the engine does not always say so at startup. godot-refcheck now runs in CI without downloading the engine and uploads SARIF. 198 files, 57 references, clean. | `godot-refcheck kanca` → 0<br>`gh api repos/Furkiozknn/kanca/actions/runs/35756474830` → 0 |
| [derin-kazi](https://github.com/Furkiozknn/derin-kazi) | advanced | A gate the game's own suite could not be: its tests measure play, not whether every scene still points at a file that exists, and the engine does not always say so at startup. godot-refcheck now runs in CI without downloading the engine and uploads SARIF. 177 files, 46 references, clean. | `godot-refcheck derin-kazi` → 0<br>`gh api repos/Furkiozknn/derin-kazi/actions/runs/35756451269` → 0 |
| [godot-refcheck](https://github.com/Furkiozknn/godot-refcheck) | advanced | Wiring the Action into four games found a bug in it in under a minute, and the wrong way round: it read its counts out of prose a clean project never prints, so it failed on exactly the repositories that were fine. Counts now come from --json, and a new gate runs the action's own shell across clean and broken fixtures at every fail-on level. | `python3 tools/action_smoke.py` → 0<br>`git stash && python3 tools/action_smoke.py` → 1<br>`cargo test` → 0<br>`gh api repos/Furkiozknn/godot-refcheck/actions/runs/35756306176` → 0 |
| [repo-vet](https://github.com/Furkiozknn/repo-vet) | advanced | The Action scanned the same repository three times - once for the JSON, once for the summary, once for the exit code - re-checking every outbound link each pass. Three passes over a flaky network can disagree, so the summary could say 'no findings' next to a failed job. --json-out makes one scan produce all three, and the self job now asserts the count and the exit code agree. | `python3 -m pytest tests -q` → 0<br>`repo-vet o/r --json-out r.json --markdown --fail-on error` → 1<br>`gh api repos/Furkiozknn/repo-vet/actions/runs/35757494256/jobs` → 0 |
| [mcp-census](https://github.com/Furkiozknn/mcp-census) | advanced | A census of a moving registry measured once is a photograph. The counts now become a time series: a monthly workflow downloads, counts, diffs the previous count metric by metric into the step summary, and appends one row to veri/zaman-serisi.csv - committing nothing when the numbers have not moved. An unmeasured metric stays blank rather than becoming zero. | `python3 -m pytest tests -q` → 0<br>`python3 -c 'analyze.sayim_farki(...)'` → 0<br>`gh api repos/Furkiozknn/mcp-census/actions/runs/35757487158` → 0 |
| [repo-ratchet](https://github.com/Furkiozknn/repo-ratchet) | advanced | The engine corrected two of its own measurements, both forced by repositories it misread: test_count reported four shipped games as having no tests because GDScript has no framework and they hand-roll assertions, and todo_density counted the pattern it searches with. Round 1's first nine records are committed with the checks that back them. | `python3 -m pytest -q` → 0<br>`python3 -m ratchet survey --only tek-tus-kosu,yercekimi-cevir,kanca,derin-kazi` → 0<br>`gh api repos/Furkiozknn/repo-ratchet/actions/runs/35756865124/jobs` → 0 |
| [mcp-vet](https://github.com/Furkiozknn/mcp-vet) | advanced | Running it against the account's own MCP servers found the worst kind of false positive for a security tool: it punished the defensive pattern. A server whose only mention of .netrc is a denylist refusing to index it was rated HIGH with DO NOT INSTALL. Two narrow contexts - denylist literals, and comments or docstrings decided by Python's own tokenizer - now stop a line setting the verdict without suppressing anything. | `python3 -m pytest tests -q` → 0<br>`mcp-vet audit --offline --path local-notes-search-mcp` → 0<br>`python3 -c 'neutralise finding_sets_headline and re-score the defensive fixture'` → 0<br>`mcp-vet audit --offline --path <fixture reading ~/.ssh/id_rsa and posting it>` → 3<br>`gh api repos/Furkiozknn/mcp-vet/actions/runs/35758412088` → 0 |
| [local-notes-search-mcp](https://github.com/Furkiozknn/local-notes-search-mcp) | advanced | A security gate the test suite could not be: the tests measure whether the functions are correct, not where the server's capability ends. mcp-vet now audits it from source on every push and writes the report into the job summary; today's verdict is NOT_FLAGGED. Getting the gate right took two commits - the first died under the runner's bash -e before its own exit-code logic could run. | `mcp-vet audit --offline --path local-notes-search-mcp` → 0<br>`bash -e -o pipefail <the gate step, verbatim>` → 0<br>`bash -e -o pipefail <the gate step against a fixture that exfiltrates an SSH key>` → 1<br>`gh api repos/Furkiozknn/local-notes-search-mcp/actions/runs (push, default branch)` → 0 |
| [mini-creative-toolkit](https://github.com/Furkiozknn/mini-creative-toolkit) | advanced | A security gate the test suite could not be: the tests measure whether the functions are correct, not where the server's capability ends. mcp-vet now audits it from source on every push and writes the report into the job summary; today's verdict is MEDIUM. Getting the gate right took two commits - the first died under the runner's bash -e before its own exit-code logic could run. | `mcp-vet audit --offline --path mini-creative-toolkit` → 1<br>`bash -e -o pipefail <the gate step, verbatim>` → 0<br>`bash -e -o pipefail <the gate step against a fixture that exfiltrates an SSH key>` → 1<br>`gh api repos/Furkiozknn/mini-creative-toolkit/actions/runs (push, default branch)` → 0 |
| [nvidia-nim-mcp](https://github.com/Furkiozknn/nvidia-nim-mcp) | advanced | A security gate the test suite could not be: the tests measure whether the functions are correct, not where the server's capability ends. mcp-vet now audits it from source on every push and writes the report into the job summary; today's verdict is LOW. Getting the gate right took two commits - the first died under the runner's bash -e before its own exit-code logic could run. | `mcp-vet audit --offline --path nvidia-nim-mcp` → 1<br>`bash -e -o pipefail <the gate step, verbatim>` → 0<br>`bash -e -o pipefail <the gate step against a fixture that exfiltrates an SSH key>` → 1<br>`gh api repos/Furkiozknn/nvidia-nim-mcp/actions/runs (push, default branch)` → 0 |
| [voice-io-mcp](https://github.com/Furkiozknn/voice-io-mcp) | advanced | A security gate the test suite could not be: the tests measure whether the functions are correct, not where the server's capability ends. mcp-vet now audits it from source on every push and writes the report into the job summary; today's verdict is NOT_FLAGGED. Getting the gate right took two commits - the first died under the runner's bash -e before its own exit-code logic could run. | `mcp-vet audit --offline --path voice-io-mcp` → 0<br>`bash -e -o pipefail <the gate step, verbatim>` → 0<br>`bash -e -o pipefail <the gate step against a fixture that exfiltrates an SSH key>` → 1<br>`gh api repos/Furkiozknn/voice-io-mcp/actions/runs (push, default branch)` → 0 |
| [masal](https://github.com/Furkiozknn/masal) | advanced | The manifest advertised an installable standalone app with no service worker behind it: most browsers never offered the install, and offline showed a blank page - for a bedtime story app on a tablet, offline is the typical case. Adds a service worker, real square app icons (the only one was a 1200x630 social image no launcher can use), and a gate proving the worker originates no request of its own and that its cache list matches the real import graph. | `node arac/sw-dogrula.mjs` → 0<br>`node arac/sw-dogrula.mjs (after injecting fetch("https://tracker.example.net/...") into sw.js)` → 1<br>`node --test` → 0<br>`grep -nE 'fetch\(|sendBeacon|...' index.html app.js ...` → 1<br>`gh api repos/Furkiozknn/masal/actions/runs (push, main)` → 0 |
| [ai-workflow-engine](https://github.com/Furkiozknn/ai-workflow-engine) | advanced | Every test drove a handwritten fake of ai-job-gateway, so two projects that advertise compatibility each only proved it against their own belief. Eight tests now run the real gateway in-process over ASGI, and found drift straight away: the gateway emits status 'pending' before 'processing' and the fake never produced it, leaving that branch of the poll loop unexercised. Plus a check that the vendored gateway_poll.py has not diverged from its source. | `python3 -m pytest tests/test_gercek_gateway.py -q` → 0<br>`python3 -m pytest -q` → 0<br>`bash -e -o pipefail <the contract step> with ai-job-gateway uninstalled` → 5<br>`python3 arac/vendor-dogrula.py --dosya <canonical>` → 0<br>`gh api repos/Furkiozknn/ai-workflow-engine/actions/runs` → 0 |
| [ai-job-gateway](https://github.com/Furkiozknn/ai-job-gateway) | advanced | gateway_poll.py is copied byte-identically into three other repositories and nothing measured whether they were still identical; an edge case fixed here would keep biting there while every repository stayed green against its own copy. The three consumers now compare against this one, and this one prints the fingerprint so a change is visible as a number. | `python3 arac/vendor-dogrula.py --dosya <canonical>` → 0<br>`python3 arac/vendor-dogrula.py (after changing one line in a copy)` → 1<br>`gh api repos/Furkiozknn/ai-job-gateway/actions/runs` → 0 |
| [model-comparison-harness](https://github.com/Furkiozknn/model-comparison-harness) | advanced | gateway_poll.py is a verbatim copy from ai-job-gateway and nothing checked it was still current. Copies drift in silence: an edge case fixed upstream keeps biting here while this repository stays green against its own stale copy. A gate now fetches the canonical file, normalises the package-name difference and fails on anything else - and skips rather than passes when it cannot look. | `python3 arac/vendor-dogrula.py --dosya <canonical>` → 0<br>`python3 arac/vendor-dogrula.py (after changing one line in a copy)` → 1<br>`gh api repos/Furkiozknn/model-comparison-harness/actions/runs` → 0 |
| [prompt-template-manager](https://github.com/Furkiozknn/prompt-template-manager) | advanced | gateway_poll.py is a verbatim copy from ai-job-gateway and nothing checked it was still current. Copies drift in silence: an edge case fixed upstream keeps biting here while this repository stays green against its own stale copy. A gate now fetches the canonical file, normalises the package-name difference and fails on anything else - and skips rather than passes when it cannot look. | `python3 arac/vendor-dogrula.py --dosya <canonical>` → 0<br>`python3 arac/vendor-dogrula.py (after changing one line in a copy)` → 1<br>`gh api repos/Furkiozknn/prompt-template-manager/actions/runs` → 0 |
| [claude-code-intelligence](https://github.com/Furkiozknn/claude-code-intelligence) | advanced | docs/PROVIDERS.md ucuncu taraflara kendi adaptorunu yazmayi teklif ediyor; o teklifin dayandigi cci/adapters/ yuzeyindeki 26 genel sembolun hicbirinin belgesi yoktu. Belgeler imzayi degil karari anlatiyor: Capabilities'i durust doldurma zorunlulugu, Health.status'un neden uc degerli oldugu, credentials_path'in neden var olup hic acilmadigi. arac/sozlesme-belgeli.py bunu AST uzerinden, yalnizca cci/adapters/ kapsaminda bir CI kapisina cevirdi. | `python3 -m pytest -q` → 0<br>`python3 arac/sozlesme-belgeli.py` → 0<br>`python3 arac/sozlesme-belgeli.py  # negatif kontrol: Health'in belge dizgesinin onune x = 1` → 1<br>`gh api .../actions/runs/35762137176/jobs` → 0 |
| [asset-provenance-toolkit](https://github.com/Furkiozknn/asset-provenance-toolkit) | advanced | README'nin 'dogal sonraki adim, sonraya birakildi' dedigi MP4/QuickTime arka ucu yazildi. Kayit bir uuid kutusuna, medya verisinin arkasina ekleniyor: moov icindeki stco/co64 mdat'a mutlak konum tuttugu icin oncesine sokusturulan bir kutu, ayristirilan ama cozulmeyen bir dosya birakiyor -- bu negatif kontrol olarak suitte duruyor. Korunan bolgedeki bir bayti oynatmasi gereken her duzenleme reddediliyor. 86 -> 125 test; ayrica gercek ffmpeg kliplerinde cozulmus ciktinin sha256'sini karsilastiran bir CI isi, ve pyproject'in iddia ettigi uc Python surumu icin matris. | `python3 -m pytest -q` → 0<br>`uv run --python 3.11/3.12/3.13 pytest -q` → 0<br>`python3 arac/gercek-video-dogrula.py` → 0<br>`python3 arac/gercek-video-dogrula.py  # arka uc sabote edilmis (kutu en basa)` → 1<br>`aprov embed/extract/verify clip.mp4 (README komutlari)` → 0<br>`gh api .../actions/runs/35763332016/jobs` → 0 |
| [nova-drift](https://github.com/Furkiozknn/nova-drift) | advanced | Uc kontrol de aslinda kontrol etmiyordu. (1) prng.spec.js Gunluk Meydan Okuma'nin RNG'sini script.js'ten KOPYALAYIP kopyayi sinliyordu; rng.js ayri bir modul olarak cikarildi ve test artik gonderilen dosyanin baytlarini kosuyor. (2) scripts/vendor-three.mjs  ile basliyordu ve vendor/README.md'nin tarif ettigi komut, styles.css'in yukledigi Orbitron woff2'sini siliyordu -- sessizce, cunku font-display: swap arkasinda sayfa yuklenmeye devam ediyor. (3) fixtures.js olmayan bir CDN'i cevapliyordu; yeniden eklenen bir CDN import'unu maskeleyebilirdi, artik sayfayi kendi kaynagina muhurluyor. 28 -> 38 test, ve CI matrisi iki Node surumu kosuyor. | `npx playwright test` → 0<br>`npx playwright test  # node 20.19.0 indirilip` → 0<br>`node arac/../scripts/vendor-three.mjs  # eski rm -rf vendor/ hali geri konularak` → 1<br>`node test/capture-gif.js /tmp/cap 24` → 0<br>`tarayicida document.fonts.check('700 1rem Orbitron')` → 0<br>`gh api .../actions/runs/35769805516/jobs` → 0 |
| [ajans-os](https://github.com/Furkiozknn/ajans-os) | advanced | Sozlesme dogrulayicisinin 106 kontrolluk bir oz-testi vardi ve 'temiz' diyordu; bu kontrollerin gectigini soyler, hangi kurallarin korundugunu soylemez. arac/sema-mutasyon.js her zorlama noktasini tek tek etkisizlestirip oz-testin ve ornek dogrulamasinin bunu yakalamasini bekliyor: 19 noktanin 8'i hayatta kaliyordu -- type dahil. Dogrulayicinin kendi mekanigini sinayan bir blok eklendi, olcum 19/19. Ayrica sema-dogrula ve kanit-dogrula-test hic CI'da kosmuyordu (106 kontrolluk oz-test, sifir push); kapiya baglandi ve kapinin kostugunu kanitlayan bir adim eklendi. kanit-dogrula-test'in sessiz atlamasi artik ozette sayiliyor, kanit-dogrula.js'in sabit D:/Repolar yolu ortam degiskenine cikti. | `npm run kapi` → 0<br>`node arac/sema-mutasyon.js  # oz-test eklenmeden once` → 1<br>`node arac/sema-mutasyon.js  # oz-test eklendikten sonra` → 0<br>`node arac/sema-dogrula.js --test` → 0<br>`npm run kapi  # sema adimi package.json'dan dusurulmus` → 0<br>`node arac/kanit-dogrula-test.js --kati` → 1<br>`gh api .../actions/runs/35769963799/jobs` → 0 |
| [Furkiozknn](https://github.com/Furkiozknn/Furkiozknn) | advanced | Tek bir rakam yedi yuzeyde duruyordu ve hepsi elle tutuluyordu; gunluk denetim ayrismayi goruyor ama duzeltemiyor, ve TESTLER.md'nin kendi tablo satirlarina hic bakmiyordu. schema/testler.py o bosluga bakiyor: --kontrol agsiz bir CI kapisi, --tazele/--yaz duzeltir, --olc her sayinin geldigi kosu logunu yeniden okur. Yol boyunca uc gercek hata cikti: kalibin ILK eslesmesini almak coklu-isli bir logda yanlis sayiyi veriyordu (ai-workflow-engine 72 yerine 8), GitHub'in 403 hiz siniri 'ci.yml yok' gibi gorunuyordu, ve hero.svg'de commit sayisi etiketiyle ust uste biniyordu (iki render motorunda da dogrulandi). Yayimlanan toplam 4.700 -> 5.037, 23 -> 25 suit, 27 -> 28 depo, 1.019 -> 1.067 commit. | `python3 schema/test_schema.py` → 0<br>`python3 schema/testler.py --kontrol` → 0<br>`python3 schema/testler.py --kontrol  # TESTLER tablosunda bir satir bozulmus` → 1<br>`python3 schema/testler.py --kontrol  # buradane kaynagi yanlis etiketlenmis` → 1<br>`DEPO_JETONU=... python3 schema/testler.py --olc` → 1<br>`python3 schema/dogrula.py <kok>` → 0<br>`cairosvg + chromium ile assets/hero.svg render edildi` → 0<br>`gh api .../actions/runs/35773057688/jobs` → 0 |

### Round 2

6 repositories over 7 records, 7 advanced, 49 checks recorded, 49 returned what they were run to return. 13 of them are negative controls - checks recorded because they had to fail.

| Repository | Outcome | What changed / why not | Verified by |
| --- | --- | --- | --- |
| [repo-ratchet](https://github.com/Furkiozknn/repo-ratchet) | advanced | Turun kendi verisi uc seyi gosterdi. (1) "<ad>-test.js" bir test dosyasidir: ayiricilardan ucuncusu bilinmiyordu ve sekiz hand-rolled kosucuyla korunan 70 ajanlik bir kadro "0 test bytes in 0 files" olarak olculup tur 2 kuyrugunda birinci siraya yerlesiyordu -- oraya koyan sey depo degil, olcumdu. (2) Sayilamayan bir test dosyasi sifir vaka degildir: artik ya None ya "en az N vaka; M dosya sayilamadi". Ayrim ince ve bilerek -- tests/ altinda yasamak bir dosyayi suitin parcasi yapar, vaka tasiyan bir test dosyasi yapmaz. (3) Tur 1'de 106 kontrolun 23'u basarisiz olmak icin kosulmustu ve kayit bunu soyleyemiyordu; Verification artik beklenen cikis kodunu tasiyor (expect), kural 2 ozunde ayni kaldi: bir advanced kaydi hala basarili olmasi gereken en az bir kontrola dayanmak zorunda. | `python3 -m pytest -q` → 0<br>`python3 -m ratchet survey --only turkce-ajanlar  # duzeltmeden once` → 0<br>`python3 -m ratchet survey --only turkce-ajanlar  # duzeltmeden sonra` → 0<br>`python3 -m ratchet queue --round 2` → 0<br>`gh api .../actions/runs/35773956468/jobs` → 0 |
| [buradane](https://github.com/Furkiozknn/buradane) | advanced | Tur 1 docs/api-sozlesme-farklari.md'yi calistirilabilir yapmis ve ilk kosusunda gercek bir eksik bulmustu; tur 2 o eksigi kapatiyor. Iki OSM etiketi -- access ve operator -- frontend ikisine de bagimliydi, backend hicbir semada uretmiyordu. Ikisi de artik modelde, c3f8a1d05b47 migration'inda ve PlaceListItem'da, yani listede: frontend karti cizmeden once access'e bakip eliyor ve isaretci basina bir detay istegi gerektiren bir filtre filtre degildir. Ice aktarim ikisini de OSM'in kendi etiketlerinden dolduruyor; access enum degil duz metin, cunku gorulmemis bir deger bir ilin alimini dusurmek yerine veritabanina ulasmali. Migration geri doldurma yapmiyor -- sutunlar var olmadan once alinmis 167.829 satir icin durust deger "bilinmiyor". Degisiklik yapildiginda belge testi kendiliginden kirmizi yandi ve belgeyi guncelletti; arkasindan ayristiricinin kendi testi kirildi, cunku icinde sabit bir ornek alan adi yaziyordu. Kapanmayan kisim belgede duruyor: access'e gore sorgu tarafi eleme hala yok. | `uv run pytest -q  (backend, yerel)` → 0<br>`uv run pytest -q  # belge guncellenmeden once` → 1 (had to)<br>`uv run pytest -q tests/test_sozlesme_dokumani.py  # migration dosyasi kaldirilmis` → 1 (had to)<br>`uv run pytest -q tests/test_osm_etiketleri.py` → 0<br>`gh api .../actions/runs/35774493658/jobs` → 0 |
| [masal](https://github.com/Furkiozknn/masal) | advanced | README "Lighthouse mobil: erisilebilirlik ... 100" diyordu ve bu dogruydu -- Lighthouse'un olctugu ekranda. Lighthouse sayfayi acar ve acildigi haliyle denetler; burada acilan ekran formdur, cocugun masali okudugu ekran hic denetlenmemisti. Denetlenince iki WCAG 2.1 AA ihlali cikti, ikisi de okuma ekraninda: boyama ipucu (beyaz uzerinde 3,82:1) ve cocugun kendi adi (.metin b, kagit uzerinde 4,37:1, esik 4,5) -- sayfadaki en cok okunan kelime. index.html'in o satirindaki yorum zaten "kagit uzerinde 4.37:1" yaziyordu: olculmus, yazilmis, oyle birakilmis. Ikisi de duzeltildi ve asil mesele kapiya baglandi: arac/erisim-denetle.mjs uygulamayi gercekten aciyor, formu dolduruyor, sayfayi ceviriyor, boyuyor, secim yapiyor ve sona gidiyor; alti ekrani ayri ayri axe-core ile denetliyor. Betigin kendi ilk surumu de ayni hataya dustu (boyanabilir sayfada durup secim ekranini hic denetlemiyordu) ve o da duzeltildi. | `node arac/erisim-denetle.mjs` → 0<br>`node arac/erisim-denetle.mjs  # eski --vurgu rengi geri konarak` → 1 (had to)<br>`axe-core, okuma ekraninda, duzeltmeden once` → 1 (had to)<br>`node --test` → 0<br>`tarayicida ekran goruntusu alinip bakildi` → 0<br>`gh api .../actions/runs/35775535953/jobs` → 0 |
| [godot-refcheck](https://github.com/Furkiozknn/godot-refcheck) | advanced | Baglanti cozucusu, cogu projenin hic yazmadigi bir dosya bolumune bakiyordu: bu hesaptaki dort oyunun hic [connection] blogu yok, onun yerine GDScript'te 145 .connect( cagri noktasi var. Yeni kontrol missing-node-path, ayni iddiayi yazildigi yerde yarguluyor -- betigin icindeki $Head/Body ve get_node("Head/Body"). Yanlis oldugunda motor null donuyor ve bir sonraki satir calisma aninda, oraya varan dalda oluyor. Sessizlik olculerek kuruldu: aliciyi gormezden gelmek tek bir oyunda 132 bulgu uretiyordu (kendi kurdugu sahneye uzanan headless test kosucusu), calisma aninda adlandirilan bir dugumu eksik saymak da son yanlis pozitifi (alet.name = "Aletler" + add_child). Bes gercek projede (~850 dosya) artik hicbir sey bildirmiyor; isirdigi yer yeni fikstur. 107 -> 128 test, ve CI'in headless Godot ile karsilastirma isi de gecti. | `cargo test` → 0<br>`cargo clippy --all-targets -- -D warnings; cargo fmt --check` → 0<br>`godot-refcheck <bes gercek Godot projesi> --recursive` → 0<br>`godot-refcheck yercekimi-cevir  # alici kontrolu eklenmeden once` → 1 (had to)<br>`godot-refcheck derin-kazi  # calisma-ani ad kurali eklenmeden once` → 1 (had to)<br>`godot-refcheck tests/projects/nodepath` → 1 (had to)<br>`gh api .../actions/runs/35776712648/jobs` → 0 |
| [godot-refcheck](https://github.com/Furkiozknn/godot-refcheck) | advanced | Ayni turda ikinci kayit, cunku ilkinin dayandigi olcum eksikti. missing-node-path bu hesabin bes Godot projesinde olculmus ve temiz cikmisti; deponun kendi kalibrasyon korpusuna -- onbir bakimli ucuncu taraf deposu, 237 proje -- hic kosulmamisti. Kosulunca 15 dugum bulgusu verdi ve besi yanlisti. Ikisi de ayni kor nokta: add_child(sahne.instantiate()) ile eklenen cocuk O sahnenin KOK dugumunun adini alir (godot-demo-projects'te PackedScene parametresiyle geliyor, dialogic'te disa aktarilmis diziyle), ve has_node("X") ile korunan bir yol zaten yazarin "olmayabilir" dedigi yerdir. Ayrica bir satirda ayni yol iki kez yaziliysa iki ofset var ama yargilanacak tek iddia -- material-maker'in paint.gd:910 satiri kelimesi kelimesine iki kez basiliyordu. Korpus artik 50 bulgu bildiriyor (41 + 9 dugum), dokuzu da elle dogrulandi ve docs/corpus.md'de tek tek yaziyor; resmi demo projelerindeki ayni kusur hem 4.x hem 3.x dalinda duruyor. Asil mesele kapiya baglandi: haftalik Corpus isi tabloyu basip ne bulursa bulsun yesil kaliyordu, yani bu siniftaki bir gerileme onu hic kirmazdi. Artik --expect docs/corpus.md ile beklenen toplami belgenin kendi satirindan okuyor; sayi ile belge birbirinden ayri kayamiyor. 128 -> 133 test. | `cargo test` → 0<br>`cargo clippy --all-targets -- -D warnings; cargo fmt --check` → 0<br>`python3 tools/corpus.py --expect docs/corpus.md` → 0<br>`python3 tools/corpus.py --expect docs/corpus.md  # belgeye 49 yazilmis` → 1 (had to)<br>`godot-refcheck tests/projects/nodepath  # bubble.tscn'in kok adi degistirilmis` → 1 (had to)<br>`godot-refcheck tests/projects/nodepath  # has_node koruyucusu kaldirilmis` → 1 (had to)<br>`godot-refcheck <bes gercek Godot projesi> --recursive` → 0<br>`gh api .../actions/runs/35778919844/jobs` → 0<br>`gh api .../actions/runs/35778962285  # Corpus is akisi, elle tetiklendi` → 0 |
| [claude-code-intelligence](https://github.com/Furkiozknn/claude-code-intelligence) | advanced | Uzatma noktasi belgeliydi, sozlesme testi vardi, kayit defteri de vardi -- ve Registry.register() deponun tamaminda yalnizca bir testten cagriliyordu. Urunun hicbir yeri kayit defterini okumuyor, cci scan dahil her cagri yeri somut sinifi adiyla ice aktariyordu. Yani docs/EXTENDING.md'yi harfiyen izleyip bir adaptor yazan kisi, hicbir zaman cagrilmayacak bir nesne elde ediyordu; "cci saglayiciya bagli degil" cumlesinin karsiligi kodda yoktu. Sozlesme testi bunu gosteremezdi cunku gordugu adaptorler ayni dosyada, ayni elden yazilmistir -- kendi kopyasini test eden bir kontrol. Artik bir cci.adapters giris noktasi grubu var: kurulu paket build(env, home) duyuruyor, kesif aninda assert_contract kosuyor, gecmeyen yuklenmiyor ve nedeni cci providers ciktisinda yaziyor; patlayan eklenti kendini devre disi birakiyor, cekirdegi degil; birinci taraf iki adaptor de artik ayni yoldan geciyor. Ispat paket sinirinin disindan geliyor: testler adaptoru gecici bir dizine kendi .dist-info meta verisiyle yaziyor (pip install'in urettiginin aynisi) ve kaydinin olay deposuna girdigini gosteriyor, meta veri kaldirilinca hicbir sey bulunmuyor; CI ise ornekler/cci-adapter-ornek paketini gercekten kurup cci gormezse kirmizi yaniyor, sonra kaldirip listede olmadigini da dogruluyor. Ayrica Stage 15'in kendi kabul olcutu "kapsam dislama gerekcesi" istiyordu ve hicbir yerde yazili degildi: dort adaptorden ikisi var, eksik olan kod degil dogrulanmis fixture, ve schema_verified=false ile yazilan bir adaptorun urettigi her sayi bu aracin kendi olcut sisteminde inferred'a duser. 261 -> 273 test. | `uv run pytest` → 0<br>`uv pip install ./ornekler/cci-adapter-ornek && cci --json providers` → 0<br>`uv pip uninstall cci-adapter-ornek && cci --json providers` → 0<br>`uv run pytest tests/test_adapter_plugins.py  # .dist-info kaldirilmis hali` → 1 (had to)<br>`uv run pytest tests/test_adapter_plugins.py::test_scan_ingests_from_an_externally_installed_adapter` → 0<br>`uv run python arac/sozlesme-belgeli.py` → 0<br>`cci <her alt komut> --help` → 0<br>`gh api .../actions/runs/35780415530/jobs` → 0<br>`gh api .../actions/runs/35780415001/jobs` → 0 |
| [Furkiozknn](https://github.com/Furkiozknn/Furkiozknn) | advanced | --kontrol ile --olc ayni soruyu sormuyor ve fark bu deponun butun meselesi. --kontrol manset gorseli, rozet, tablo ve toplamlarin birbiriyle tutarli olup olmadigina bakiyor; hepsi tutarli ve hepsi bayat olabilir -- yanlis bir sayinin dort kopyasi hala yanlis bir sayidir. Yalnizca --olc kosuya geri gidiyor, ve --olc'yi hicbir sey kosmuyordu: CI yalnizca --kontrol'u kosuyor, gunluk denetim ise o kontrolu yapabilecek jetonu (DEPO_JETONU) hic almiyordu ve jeton yokken kontrolu SESSIZCE atliyordu. Yani rapor, sistemin kendisi hakkinda soyledigi en yuklu sayiya hic bakilmamis gunlerde de "temiz" yaziyordu. Artik denetim raporu kac deponun sayisinin gercekten kosuya karsi dogrulandigini ve kacina bakilamadigini nedeniyle birlikte en basta yaziyor -- bakilamayan bir sayi temiz degildir -- ve denetim.yml jetonu geciyor. Elle kosulan olcum ucunu birden buldu: godot-refcheck 107 -> 133, claude-code-intelligence 261 -> 273, ve repo-ratchet'in yayimlanan kaynagi `130 passed` kendi CI logunda hic gecmiyordu, cunku is akisi pytest'i -q ile kosuyor ve -q altinda pytest yalnizca ilerleme noktalarini basiyor. Sayi dogruydu, kaynagi orada degildi: kimse geri gidip dogrulayamazdi, ve cevrimdisi kontrol o sirada yesildi. Ucu de duzeltildi ve duzeltmeden sonra dordu birden kendi kosusuna karsi dogrulandi. 5.062 -> 5.101 test, 1.067 -> 1.081 commit; bu deponun kendi suiti 68 -> 75. | `python3 schema/test_schema.py` → 0<br>`python3 schema/testler.py --olc --bekleme 2` → 1 (had to)<br>`python3 schema/testler.py --kontrol  # ayni anda, olcumden once` → 0<br>`gh run view <repo-ratchet ci.yml> --log | grep -c 'passed'` → 1 (had to)<br>`python3 schema/testler.py --tazele --depo-sayisi 28 --commit-sayisi 1081` → 0<br>`python3 schema/testler.py --olc --depo repo-ratchet --depo godot-refcheck --depo claude-code-intelligence --depo masal` → 0<br>`python3 schema/dogrula.py <kok>` → 0<br>`gh api .../actions/runs/35781758981/jobs` → 0 |

